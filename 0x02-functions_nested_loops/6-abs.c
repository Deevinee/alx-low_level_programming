#include "main.h"
/**
 * _abs - compute the absloute value of an integer
 * @t: the integer to be computed
 * Return: the absloute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
